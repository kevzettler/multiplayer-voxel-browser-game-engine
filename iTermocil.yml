windows:
  - name: client
    commands:
      - npm run dev-client
  - name: server
    layout: even-vertical
    panes:
      - sleep 5 && npm run dev-server
      - sleep 10 && nodemon --inspect build/server.js
  - name: signalhub
    commands:
      - npx signalhub listen -p 8080
